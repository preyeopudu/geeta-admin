import React from "react";
import PackageList from "../sections/packageList/packageList";

const HomePage: React.FC = () => {
  // Dummy functions for onEdit and onDelete

  return (
    <div>
      <PackageList />
    </div>
  );
};

export default HomePage;
